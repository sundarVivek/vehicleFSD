import { SearchServicePipe } from './search-service.pipe';

describe('SearchServicePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchServicePipe();
    expect(pipe).toBeTruthy();
  });
});
